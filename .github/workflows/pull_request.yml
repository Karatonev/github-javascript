name: Trigger by a pull request
on:
  pull_request_target:
    branches:
      - main
jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - name: PR Information
        run: echo "${{ github.event.number }}"